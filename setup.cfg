[egg_info]
# Workaround for filesystems where setuptools cannot create temp files under
# <project>/*.egg-info (common on some shared mounts).
# Redirect egg-info generation to a local temp directory.
egg_base = /tmp
