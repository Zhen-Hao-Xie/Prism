# PRISM documentation (static site)

Open **`index.html`** in a browser, or serve locally:

```bash
cd docs
python -m http.server 8080
# http://localhost:8080
```

For GitHub Pages: set **Source** to the `/docs` folder on your default branch; the site will be served from `docs/index.html`.
